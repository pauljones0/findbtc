# Winget manifests for findbtc 9.9.9 (bootstrap; URLs + SHA-256 verified
# against the release checksums.txt). Prepared for manual submission to
# microsoft/winget-pkgs — that external PR stays pending per Goal 37, so
# these install today only via `winget install --manifest <this dir>`.
# Layout mirrors the community repo: manifests/p/pauljones0/findbtc/9.9.9/.
PackageIdentifier: pauljones0.findbtc
PackageVersion: 9.9.9
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.6.0
