# findbtc fish completion — GENERATED by `findbtc -gen-completion=fish`; do not edit.
complete -c findbtc -o advise -r -d 'Inspect TARGET and print the recommended scan command with reasons (never scans, never runs anything)'
complete -c findbtc -o balance-endpoint -r -d 'Opt-in: query ADDRESS balances from this Esplora-compatible base URL (only with -watch; leaks addresses to that server)'
complete -c findbtc -o baseline -r -d 'Suppress -walk/-patch findings fingerprinted in baseline FILE (a reviewed hits.jsonl from an earlier sweep)'
complete -c findbtc -o case-log -r -d 'Append a JSON case-log record per scan to FILE (source identity, streaming hashes, skipped ranges, counts)'
complete -c findbtc -o checkpoint -r -d 'Journal scan progress to FILE every 1MB for crash recovery'
complete -c findbtc -o complete -d 'With -report: enumerate checksum-valid completions for partial-seed (near-miss) hits (needs --reveal; local terminal only)'
complete -c findbtc -o complete-max -r -d 'With -report -complete: candidates shown per hit, most-likely first (only with -complete)'
complete -c findbtc -o complete-out -r -d 'With -report -complete: write candidate account keys (watch-only) to PATH for -watch (only with -complete)'
complete -c findbtc -o context -r -d 'Bytes carved before and after each hit (only with -extract-dir)'
complete -c findbtc -o dfxml -r -d 'Convert a -json hits file (or - for stdin) to DFXML on stdout instead of scanning'
complete -c findbtc -o extract-dir -r -d 'Carve bytes around each hit into DIR/hit-NNNNNN.bin plus a .json sidecar (never on the scanned device itself)'
complete -c findbtc -o fail-on-hit -d 'Exit 3 when the scan or report finds anything (for CI gates and pre-commit hooks); without it, finding hits still exits 0'
complete -c findbtc -o fs -r -d 'Filesystem-aware mode: recover deleted entries with names from the NTFS/ext/FAT/exFAT volume in FILE and scan their content'
complete -c findbtc -o fs-offset -r -d 'Byte offset of the volume boot sector inside FILE (only with -fs and -unallocated-only); when omitted, the partition table is followed automatically'
complete -c findbtc -o gen-completion -r -d 'Write a shell completion script for bash, zsh, or fish to stdout and exit (generated from the flag table; scan inputs still refuse)'
complete -c findbtc -o gen-man -d 'Write the findbtc man page (troff) to stdout and exit (OPTIONS generated from the flag table; scan inputs still refuse)'
complete -c findbtc -o hashes -r -d 'Extract crack-ready password hashes from FILE (or - for stdin) instead of scanning'
complete -c findbtc -o json -d 'Print detections as JSON lines instead of human text'
complete -c findbtc -o patch -d 'Parse input as a patch series (git log -p): attribute hits to commit+path with history fingerprints for -baseline'
complete -c findbtc -o profile -r -d 'Detector set: empty (wallet matchers) or secrets (adds private-key blocks and credential shapes; see docs/SECRETS_PROFILE.md)'
complete -c findbtc -o report -r -d 'Summarize a -json hits file (or - for stdin) instead of scanning'
complete -c findbtc -o resume -d 'Resume from -checkpoint instead of scanning from the start'
complete -c findbtc -o reveal -d 'Print seed words for BIP39 hits (owner recovery only; NEVER share this output)'
complete -c findbtc -o s -r -d 'Start at byte offset'
complete -c findbtc -o salvage -r -d 'Analyze FILE for salvageable database pages instead of scanning'
complete -c findbtc -o salvage-out -r -d 'Write the salvaged database image to PATH (only with -salvage)'
complete -c findbtc -o targets -r -d 'Read more scan targets from FILE (one path per line; blank lines and # comments ignored) in addition to positionals (flags must precede targets)'
complete -c findbtc -o tokenlist -r -d 'Build a BTCRecover tokenlist from the words in FILE, a carve, or hits.jsonl with carves (or - for stdin) instead of scanning'
complete -c findbtc -o tokenlist-max -r -d 'Emit at most N token lines, first-seen first (only with -tokenlist)'
complete -c findbtc -o tokenlist-out -r -d 'Write the tokenlist to PATH instead of stdout (only with -tokenlist)'
complete -c findbtc -o unallocated-only -d 'Scan only unallocated filesystem space (volume at -fs-offset); skips live data'
complete -c findbtc -o verify-case-log -r -d 'Re-hash the sources behind each record in case-log FILE and report match/mismatch instead of scanning'
complete -c findbtc -o version -d 'Print version and exit'
complete -c findbtc -o walk -r -d 'Sweep every regular file under DIR with the standard detectors (symlinks not followed unless -walk-follow-symlinks)'
complete -c findbtc -o walk-follow-symlinks -d 'Follow symlinks during -walk (loops fail safe per link)'
complete -c findbtc -o walk-maxdepth -r -d 'Descend at most N levels below DIR during -walk (0 = unlimited)'
complete -c findbtc -o watch -r -d 'Derive watch-only addresses from extended public keys in FILE (a carve, a key file, or hits.jsonl with carves) instead of scanning'
complete -c findbtc -o watch-count -r -d 'Addresses to derive per chain, external and change (only with -watch)'
complete -c findbtc -o watch-format -r -d 'Address-list format: csv or json (only with -watch)'
complete -c findbtc -o watch-out -r -d 'Write the address list to PATH instead of stdout (only with -watch)'
